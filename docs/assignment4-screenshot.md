# Устарело

Раньше здесь было описание скриншота Pull Request для другой версии задания 4.

**Сейчас задание 4** — Dockerfile и `docker-compose.yml` с PostgreSQL и Alembic.

Для отчёта достаточно:

1. Успешный запуск `docker compose up --build`
2. Открытый http://localhost:8000/docs
3. При необходимости — скриншот терминала с `pytest` (задание 2)

Подробности: [README.md](../README.md), раздел «Docker (задание 4)».
