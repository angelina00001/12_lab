# Задание 7 — промпты для тестов

Все промпты и результаты: **[PROMPT_LOG.md](../PROMPT_LOG.md)** (раздел «Задание 7»).

Запуск coverage:

```bash
cd backend
pytest --cov=app --cov-report=term-missing --cov-report=html
```

Отчёт: `docs/coverage-summary.txt`, HTML: `backend/htmlcov/index.html`.
